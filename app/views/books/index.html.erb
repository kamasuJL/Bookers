<h1>index</h1>

<% @books.each do |book| %>

  <p>Title</p>
  <span><%= book.title %></span>

  <p>Body</p>
  <span><%= book.body %></span>

  <p>
    <span>
      <%= link_to "Show", book_path(book.id) %>
    </span>
  </p>

<% end %>